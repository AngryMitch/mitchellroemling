# Copy this file to `.env` (which is git-ignored) for the family corner.
#
# The passcode used by `npm run encrypt` to encrypt .family/*.md.
# It is NEVER shipped to the site, only the resulting ciphertext is.
# You can also pass it inline:  npm run encrypt -- --passcode="your code"
FAMILY_PASSCODE=changeme
